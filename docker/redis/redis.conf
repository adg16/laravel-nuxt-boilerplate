# Redis backs cache, session, and queue (on separate logical DBs — see
# backend/config/database.php). noeviction + AOF persistence protect queued
# jobs and sessions from being silently dropped; bump maxmemory as needed.
maxmemory 256mb
maxmemory-policy noeviction
appendonly yes
appendfsync everysec
